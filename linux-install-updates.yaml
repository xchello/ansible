---
- name: Install updates on CentOS/RHEL servers
  hosts: test
  become: true
  become_user: root
  tasks:
    - name: Update all packages to the latest version
      yum:
        name: '*'
        state: latest
